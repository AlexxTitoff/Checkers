using System;

using ModelLibrary;

namespace ConsoleUI
{
    class Program
    {
        static void Main(string[] args)
        {
            #region TrueGame

            //ConsoleController controller = new ConsoleController();

            //UI consoleView = new UI(controller);

            //consoleView.PlotEmptyBoard();

            //consoleView.PrintMenu();

            //consoleView.PlotGame(currentGame, board);

            //int typeOfGame = 1;


            #endregion




            //TODO: init params

            //Console.ReadKey();

            //do
            //{
            //    Console.Clear();
            //    Console.WriteLine("Choose a type of Game:\n" +
            //        "1 - with a Person\n" +
            //        "2 - with the Computer");

            //    key = Console.ReadKey(true).Key;

            //    switch (key)
            //    {
            //        case ConsoleKey.D1:
            //            typeOfGame = 1;
            //            break;
            //        case ConsoleKey.D2:
            //            typeOfGame = 2;
            //            break;
            //    }

            //    if (key == ConsoleKey.D1)`
            //    {
            //        UI.ExecuteOperations(currentGame, typeOfGame, ref key);
            //    }

            //    if (key == ConsoleKey.D2)
            //    {
            //        UI.ExecuteOperations(currentGame, typeOfGame, ref key);
            //    }

            //} while (key != ConsoleKey.Escape);

            //FieldNodes fieldNodes = FieldNodes.GetInstance();

            //FieldsInitializer fieldsInitializer = new FieldsInitializer(fieldNodes);

            Console.ReadKey();
        }
    }
}
